BENCHMARK_TASKS = [
    {
        "id": "task_05_repo_feature_addition",
        "description": "Extend the multi-file shopping cart repository to support percentage discount codes. Implement an 'apply_discount(code)' method on the ShoppingCart class that reduces the total for 'SAVE20' by 20%, and raises a ValueError for unknown codes.",
        "buggy_code": "# This task uses pre-staged multi-file repository configurations inside the sandbox directory.",
        "test_suite": "# Pre-staged in sandbox/test_suite.py"
    }
]